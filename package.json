{
	"name": "ctrl",
	"version": "0.1.0",
	"description": "Hyperextensible AI-native terminal editor",
	"keywords": ["editor", "terminal", "vim", "ai", "coding", "typescript"],
	"license": "Apache-2.0",
	"repository": {
		"type": "git",
		"url": "https://github.com/ctrleditor/ctrl"
	},
	"type": "module",
	"bin": {
		"ctrl": "dist/cli/main.js"
	},
	"engines": {
		"bun": ">=1.3.6"
	},
	"scripts": {
		"dev": "bun run src/main.tsx",
		"build": "bun build src/main.tsx --outdir dist --minify",
		"build:binary": "bun build src/cli/main.ts --compile --outdir dist --outfile ctrl",
		"test": "bun test",
		"test:watch": "bun test --watch",
		"test:coverage": "bun test --coverage",
		"format": "biome format --write .",
		"lint": "biome lint --write .",
		"check": "biome check ."
	},
	"dependencies": {
		"@bunli/core": "^0.4.0",
		"@opentui/core": "^0.1.74",
		"@opentui/react": "^0.1.74",
		"react": "^19.2.3",
		"react-dom": "^19.2.3",
		"zod": "^4.3.6"
	},
	"devDependencies": {
		"@types/bun": "^1.3.6"
	}
}
